{% extends 'base_front.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="card">
        <h2>Inscription</h2>

        {{ form_start(form, { 'attr': {'class': 'admin-form'} }) }}
            {{ form_rest(form) }}
            <button type="submit" class="dd-btn dd-btn-account dd-btn-account-blue margin-top-50">S'inscrire</button>
        {{ form_end(form) }}
    </div>
{% endblock %}