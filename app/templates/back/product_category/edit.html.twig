{% extends 'base_back.html.twig' %}

{% block title %}Edition de la category {{ productCategory.name }}{% endblock %}

{% block body %}
    <h1>Edition de la category {{ productCategory.name }}</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <br>
        <input type="submit" value="Modifier" class="dd-btn dd-btn-edit">
    {{ form_end(form) }}
{% endblock %}