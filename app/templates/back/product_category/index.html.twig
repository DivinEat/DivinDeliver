{% extends 'base_back.html.twig' %}

{% block title %}{% trans %}category.list{% endtrans %}{% endblock %}

{% block body %}
    <h1 class="title">{% trans %}category.list{% endtrans %}</h1>
    {% if is_granted('ROLE_RESTAURATEUR') %}
        <a href="{{ path('admin_productcategory_new') }}" class="add"><i class="material-icons">control_point</i></a>
    {% endif %}

    <div class="col s12 m12 l6 offset-l2 center">
        <table cellpadding="1" cellspacing="1" class="admin-table">
            <thead>
                <tr>
                    <th>{% trans %}name{% endtrans %}</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for productCategory in productCategories %}
                    <tr>
                        <td>{{ productCategory.name }}</td>
                        <td>
                            <a href="{{ path('admin_productcategory_show', {id: productCategory.id}) }}" >
                                <i class="material-icons">remove_red_eye</i>
                            </a>
                            {% if is_granted('ROLE_RESTAURATEUR') %}
                                <a href="{{ path('admin_productcategory_edit', {id: productCategory.id}) }}">
                                    <i class="material-icons">mode</i>
                                </a>
                                <a href="{{ path('admin_productcategory_delete', {id: productCategory.id, token: csrf_token('delete_productcategory' ~ productCategory.name)}) }}">
                                    <i class="material-icons">delete</i>
                                </a>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
