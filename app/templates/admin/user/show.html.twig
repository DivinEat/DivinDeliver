{% extends 'base_admin.html.twig' %}

{% block title %}{% trans %}user.word{% endtrans %} {{ user.firstname }} {{ user.lastname }}{% endblock %}

{% block body %}
    <div class="w-100">
        Name : {{ user.firstname }} <br>
        Lastname : {{ user.lastname }} <br>
        Email : {{ user.email }} <br>
        Roles : {{ user.roles ? user.roles|json_encode|replace({ ('["ROLE_'): '', '"]' : '' }) : '' }}<br>
        Restaurant : {{ user.stores.first() ? user.stores.first().name : '' }} <br>

        <br>
        <div class="d-flex align-items-center justify-content-between">
            <a href="{{ path('admin_user_index') }}" class="text-dark mt-3">
                <span class="mr-2"><img class="icon rotate-180" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}"></span>
                Retour
            </a>    
            <a href="{{ path('admin_user_edit', {id: user.id}) }}" class="text-dark mt-3">
                <span><img class="icon" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}"></span>
                Éditer
            </a>
        </div>
    </div>
{% endblock %}
