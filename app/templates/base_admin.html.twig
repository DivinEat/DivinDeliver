<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}DivinDeliver{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    
    <body>
        <header class="d-flex align-items-center justify-content-between header-back header-back-p10">
            <div class="d-flex align-items-center flex-row ml-5">
                <a href="{{ path('default_index') }}"><img src="{{ asset('images/logo.png') }}" style="height: 67px"></a>
            </div>

            <div class="mr-5">
            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="btn-purple">{% trans %}logout{% endtrans %} Admin</a>
            {% endif %}
            </div>
        </header>

        <div class="row">
            <div class="col-2 sidenav">
                <ul class="nav m-2 d-flex flex-column">
                    <li class="nav-item">
                        <a class="nav-link priority-2 {{ (app.request.get('_route') == 'default_index') ? 'active' : '' }}" href="{{ path('default_index') }}">
                            <img class="icon rotate-180" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}">
                            <span style="font-size: 18px;">{% trans %}nav.back_home{% endtrans %}</span>
                        </a>
                    </li>
                    <div class="ligne"></div>
                    <li class="nav-item">
                        <a class="nav-link priority-2 {{ (app.request.get('_route') == 'back_profile_index') ? 'active' : '' }}" href="{{ path('back_profile_index') }}">
                            <span class="ml-3">{% trans %}nav.profile{% endtrans %}</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link priority-2 {{ (app.request.get('_route') == 'admin_default_index') ? 'active' : '' }}" href="{{ path('admin_default_index') }}">
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link priority-2 {{ (app.request.get('_route') == 'admin_user_index') ? 'active' : '' }}" href="{{ path('admin_user_index') }}">
                            <span>{% trans %}nav.users{% endtrans %}</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link priority-2 {{ (app.request.get('_route') == 'admin_store_index') ? 'active' : '' }}" href="{{ path('admin_store_index') }}">
                            <span>{% trans %}nav.restaurants{% endtrans %}</span>
                        </a>
                    </li>
                </ul>

                {# <ul class="mt-2 d-flex flex-column align-items-center">
                    <li class="w-100 pl-2 pr-2 mt-4">
                        <a href="{{ path('default_index') }}" class="w-100 d-flex flex-row align-items-center t-d-none">
                            <img class="icon rotate-180" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}">
                            <span class="ml-3">{% trans %}nav.back_home{% endtrans %}</span>
                        </a>
                    </li>
                    <div class="ligne"></div>

                    <li class="w-100 pl-2 pr-2 mt-2">
                        <a class="t-d-none" href="{{ path('back_profile_index') }}" class="w-100 d-flex flex-row align-items-center t-d-none">
                            <img class="icon" src="{{ asset('images/icons/icon admin mobile-hack.png') }}">
                            <span class="ml-3">{% trans %}nav.profile{% endtrans %}</span>
                        </a>
                    </li>

                    <li class="w-100 pl-2 pr-2" style="margin-top: 5rem">
                        <a href="{{ path('admin_default_index') }}" class="w-100 d-flex flex-row justify-content-between align-items-center t-d-none">
                            <span>Dashboard</span>
                            <img class="icon" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}">
                        </a>
                    </li>
                    <div class="ligne"></div>

                    <li class="w-100 pl-2 pr-2">
                        <a href="{{ path('admin_user_index') }}" class="w-100 d-flex flex-row justify-content-between align-items-center t-d-none">
                            <span>{% trans %}nav.users{% endtrans %}</span>
                            <img class="icon" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}">
                        </a>
                    </li>
                    <div class="ligne"></div>

                    <li class="w-100 pl-2 pr-2">
                        <a href="{{ path('admin_store_index') }}" class="w-100 d-flex flex-row justify-content-between align-items-center t-d-none">
                            <span>{% trans %}nav.restaurants{% endtrans %}</span>
                            <img class="icon" src="{{ asset('images/icons/icon-fleche-r-hack.png') }}">
                        </a>
                    </li>
                </ul> #}
            </div>

            <div class="col-9">
                {% for label, messages in app.flashes() %}                    
                    {% for message in messages %}
                        <div class="alert {{"alert-#{label}"}}">
                            <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
                
                <div class="container mt-5 d-flex flex-column align-items-center">
                    {% block body %}{% endblock %}
                </div>
            </div>
        </div>

        <footer>
            <span>DivinDeliver</span>
            <div class="w-100 d-flex justify-content-between">
                <div>© 2021, DivinDeliver</div>
                <div class="text-right">
                    <a href="tel:+33102030405">+33 1 02 03 04 05</a>
                    <br>
                    <a href="mailto:contact@divindeliver.fr">contact@divindeliver.fr</a>
                </div>
            </div>
        </footer>
        
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
